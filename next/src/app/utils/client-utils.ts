import "client-only";
export const clientSideFunctions = () => {
  console.log(
    "Use features of client like window object, localStorage and others"
  );
  return "This Is From Client Side Function";
};
