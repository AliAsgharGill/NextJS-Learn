/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    //  here we will give path of images from this site images.pexels.com and others
    domains: ["images.pexels.com"],
  },
};

export default nextConfig;
